/**
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
module.exports = function(controller) {

    // use a function to match a condition in the message
    controller.hears(async(message) => message.text === '!gym', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

    controller.hears(async(message) => message.text === '!throw', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

    controller.hears(async(message) => message.text === '!pickup', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

    controller.hears(async(message) => message.text === '!sprint', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

    controller.hears(async(message) => message.text === '!cardio', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

    controller.hears(async(message) => message.text === '!agility', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });    controller.hears(async(message) => message.text === '!weekly1', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

    controller.hears(async(message) => message.text === '!weekly2', async (bot, message) => {
        //@TODO tally points
        //self._points_to_add += self.GYM_POINTS
        //self._additions.append('!gym')

    });

}